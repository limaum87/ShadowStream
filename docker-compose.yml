services:
  app:
    build: .
    container_name: radio-rasp-player
    ports:
      - "10000:10000"
    devices:
      - /dev/snd:/dev/snd
    group_add:
      - audio
    environment:
      PORT: "10000"
      MPV_AUDIO_DEVICE: auto
    restart: unless-stopped
